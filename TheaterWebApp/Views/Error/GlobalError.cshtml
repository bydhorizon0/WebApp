@{
    ViewData["Title"] = "오류";
    var statusCode = ViewBag.StatusCode ?? 500;
}

<div class="container mt-5 text-center">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <h1 class="display-4 fw-bold text-danger">오류 @statusCode</h1>
                    <p class="lead mt-4 mb-3">죄송합니다. 요청하신 페이지를 처리할 수 없습니다.</p>
                    <p class="text-muted">오류가 반복되면 관리자에게 문의해주세요.</p>
                    <a href="/" class="btn btn-primary mt-3">
                        <i class="bi bi-house-door-fill"></i> 홈으로 돌아가기
                    </a>
                </div>
            </div>

        </div>
    </div>
</div>